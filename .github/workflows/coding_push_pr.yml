name: PUSH PR

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: comando comprovasion
        run: echo "Este comando se ejecuto cuando se hizo un push o un pull request a la rama main :D"