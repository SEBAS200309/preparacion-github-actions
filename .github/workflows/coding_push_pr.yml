name: PUSH PR

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  ejecutar_comandos:
    runs-on: ubuntu-latest
    steps:
      - name: comando comprovasion
        run: echo "¡El flujo de trabajo se activó por un push o pull request!"