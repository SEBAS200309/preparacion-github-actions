name: Coding Repo Secret

on:
  push:
    branches:
      - main

jobs:
  usar_secreto:
    runs-on: ubuntu-latest
    steps:
      - name: mostrar usar_secreto (enmascarado)
        run: |
          echo "El secreto es: ::add-mask:: ${{ secrets.MY_SUPER_SECRET }}"
